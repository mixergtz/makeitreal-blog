<% if @comment.persisted? %>
  $(".comments").append("<%= j render 'comment', comment: @comment %>");
  $(".input-form")[0].reset();
  $("#errors_show").hide();
<% else %>
  $("#errors_show").html("<%= j render 'shared/errors', modelo: @comment %>");
  $("#errors_show").show();
<% end %>